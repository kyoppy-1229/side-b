// The two faces the chat app draws, named once.
//
// Components import from here instead of reaching for the image files, so the
// protagonist's picture is a single setting rather than a path repeated in
// every surface that shows it.

import mizunoAvatar from '../../photo/solo/水野ヒロキ.png'
import playerAvatar from '../../photo/犬.png'

export const PLAYER_AVATAR = playerAvatar
export const MIZUNO_AVATAR = mizunoAvatar
